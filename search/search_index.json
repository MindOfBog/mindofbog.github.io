{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to MkDocs","text":"<p>Here are some things I work on in my free time.</p>"},{"location":"#using-my-projects","title":"Using My Projects","text":"<p>Generally all the stuff I make will be open source, and available for anyone to use as they wish. So if you want to fork my projects to make your own thing or need to use code from my projects or anything like that you are welcome to do so, all I ask is for credit where it is due, as I do the same when using work from others.</p>"},{"location":"#modding-projects","title":"Modding Projects","text":"<ul> <li>LBP Projects<ul> <li>LBP Asset Studio</li> <li>Bubblinator</li> <li>LBP Material Mesh Converter</li> <li>LBP Speedrun Util</li> </ul> </li> </ul>"},{"location":"Bubblinator/","title":"Bubblinator","text":"<p>page not finished.</p>"},{"location":"LBP_AS/","title":"LBP Asset Studio","text":"<p>LBP Asset Studio (f.k.a. Background Maker) is a tool that lets you create a variety of assets for LittleBigPlanet games. Originally made only to create custom Backgrounds, it's evolved into a tool that can make and edit plans (backgrounds, objects, materials, etc.) and bins (levels). The tool is still in development, so it may error or crash in some situations, if you are experiencing issues like that feel free to contact me on Discord to report the issues (please provide information on how it happened and on how to repeat it, otherwise I won't be able to do anything). Some features may not work or be available yet, like importing models and certain ThingParts, but they are planned to be added.</p> <p>Download</p> <ul> <li>Latest Release</li> <li>Version History</li> </ul>"},{"location":"LBP_AS/#credits","title":"Credits","text":"<ul> <li>Big thanks to ennuo, the author of CWLib which is the library LBP Asset Studio uses to create and interface with LBP files. Ennuo has also personally helped me a lot throughout the years, working on this tool and with other stuff too, appreciate you my .</li> </ul>"},{"location":"LBP_AS/#how-to-run","title":"How to run","text":"<ul> <li>The tool uses OpenGL, so the system you use it on needs to have a GPU that has those capabilities. Anything from the past decade should do, iGPU or dedicated, it doesn't matter.</li> <li>It is written in Java and as of version 1.7 requires JRE 11 or up to run. (future versions may require a higher runtime)</li> </ul>"},{"location":"LBP_AS/#how-to-use","title":"How to use","text":"<p>Controls &amp; Basic navigation</p> <ul> <li>To navigate 3D space use the direction keybinds(1), the movement speed can be changed(2).</li> <li>To rotate your view use the camera keybind(3) and move your mouse.</li> <li>Floating Dropdown-tabs can be dragged with Left Button and expanded with Right Button</li> <li>Combo-boxes can also be expanded with Right Button</li> </ul> <ol> <li>Keybinds can be changed under <code>Settings&gt;Controls</code> Default:<ul> <li>Forward: W</li> <li>Back: S</li> <li>Left: A</li> <li>Right: D</li> <li>Up: Space</li> <li>Down: Left Shift</li> </ul> </li> <li>Set a specific speed in <code>Settings&gt;Controls&gt;Move speed</code> or dynamically increase or decrease it by scrolling up and down.</li> <li>Keybinds can be changed under <code>Settings&gt;Controls</code> Default:<ul> <li>Camera: Middle Button</li> </ul> </li> </ol> <p>At the top of the window you will see a few tabs: <code>Scene</code>, <code>Archive</code>, <code>Project</code> and <code>Settings</code>. In order to create what you want with this tool you will have to familiarize yourself with their purposes &amp; functions. </p> SceneArchiveProjectSettings <p>Here is where you will be constructing any \"in world\" assets, anything from levels, objects to backgrounds, and in the future even more complicated stuff. Anything you make here will be used to build into <code>plans</code> or <code>bins</code>.</p> Loaded Elements List <ul> <li>On the right side of the window there will be a list of Elements, better known as \"Things\", that are in the current scene, you can select them from the list or by clicking on the models in the scene.  </li> <li>At the top of the list you have a \"Load elements from PLAN/LEVEL\" button, that allows you to load things from an existing loose <code>.plan</code> or <code>.bin</code> file.  </li> <li>You may also create a new thing completely from scratch if you need to. At the bottom of the list there is a \"New\" button, after creating an empty thing you can edit it and add parts onto it as you wish.</li> </ul> Available Assets Tab <p>In the <code>Available Assets</code> tab you have a list of all assets currently loaded(1). You may select any asset and it will automatically be placed into the scene. Primitive assets like <code>mols</code> or <code>gmats</code> will be placed in front of you, but <code>plans</code> or <code>bins</code> that have a position will be placed accordingly.</p> <p>If the list is empty, it could mean that:</p> <ul> <li>There are no game files or profiles loaded.</li> <li>Your game files or profiles are empty or corrupted.</li> <li>Your mod doesn't contain any usable assets.</li> </ul> <ol> <li>At the top there should be a search box as well as filters you can use to find assets easier.</li> </ol> Current Selection Tab <ul> <li>Once you have a selection you may edit the things you selected under this tab, you can change anything, Group, Parent and ThingParts, or even remove them or add new ones.  </li> <li>Multiple things can be edited at once. If you have multiple things selected, only the fields that your selection has in common, will be shown. For example, if you have two things selected and they don't have the same name, group or parent, those fields will be empty. And if those fields get changed, the changes will be applied to all selected things. This extends to ThingParts as well, only the parts that your selection has in common will appear in the list, and if you add, remove or edit a part, that change will be applied to your whole selection.  </li> </ul> Helpers Tab <p>A few helpful settings and templates can be found here.  </p> <ul> <li>Level borders: shows the the bounds of a LBP level, as well as layers, the first three and the 13 extra ones that were added in LBP3.  </li> <li>Pod helpers: show an approximation of the Pod and Planets. (Useful when making pod backgrounds)<ul> <li>Checkboxes for <code>LBP1</code>, <code>LBP2</code> and <code>LBP3</code>. (Yes, the locations are different fsr)</li> <li><code>Pod cam</code> moves your view approximately to where it would be in the pod.  </li> </ul> </li> <li>Thing templates: spawn a few necessary things.<ul> <li><code>Level BG</code> spawns a LevelSettings thing and a few scripts required for background creation.</li> <li><code>LBP1 Pod BG</code> spawns the planets and scripts required for LBP1 Pod background creation.  </li> </ul> </li> </ul> <p>In this tab you can load game files and profiles to use their assets in your scene.</p> <p>Loading game files</p> <ul> <li>Avoid using game files from a disc game, digital games have their files decrypted, disk games need their base game data extracted and decrypted.</li> <li>BIG Profiles are NOT profile backups, profile backups need to be decrypted/extracted to be usable so avoid using those.</li> </ul> MAPs Tab <p>Load or remove <code>.map</code>files, multiple can be loaded at once. Can be found inside the games folder under the name <code>blurayguids.map</code>, and <code>brg_patch.map</code> for update content.</p> FARCs Tab <p>Load or remove <code>.farc</code>files, multiple can be loaded at once. Can be found inside the games folder under the name <code>data_***.farc</code> (or <code>data.farc</code> for <code>LBP1</code>), and <code>cumulative_****.farc</code> (or <code>patch*.farc</code> for LBP1) for update content.</p> BIG Profiles Tab <p>Load or remove <code>bigfart</code> files, multiple can be loaded at once. Can be found inside the game directory in a folder that has the same name as the game but with <code>_USER*</code> added to the folders name. </p> Translations Tab <p>If your loaded game data contains translation files, they will be listed here. You may select your language of choice, this will give proper names to levels and plans listed in <code>Scene&gt;Available assets</code>, instead of raw file names.</p> <p>Here you will find a file tree, the files here will be exported to a <code>.mod</code> file once you're finished.  </p> <ul> <li>Folders can be collapsed and expanded by clicking on the icon on the left.</li> <li> <p>When right clicking or clicking on the three dots on the right of any entry in the tree, a context menu will appear:</p> <p>Clipboard operations</p> <p>Copying and pasting files only work within the tool, i.e. if you copy or cut a file in the file tree, you can paste it into the tool or even across different instances of the tool. BUT you cannot copy a file on your computer and paste it into the tool or vice versa.</p> Mod entry (root folder) <p>Behaves like a normal folder, however it cannot be deleted and it has an extra option:</p> <ul> <li><code>Load Asset(s) from Mod</code> allows you to load all contents of an existing mod into your project.</li> </ul> Folders <ul> <li><code>Load Asset(s)</code> lets you load loose files into the mod, they have to be LBP format files. e.g. <code>.plan</code>, <code>.mol</code>, <code>.gmat</code>, etc.</li> <li><code>Build Scene</code> is where you compile your scene to a <code>.plan</code> or <code>.bin</code> </li> <li><code>Import</code> allows you to convert and import regular format files to LBP format. e.g. <code>.png</code> -&gt; <code>.tex</code>, <code>.glb</code> -&gt; <code>.mol</code></li> <li><code>New Folder</code> creates a new folder under the selected one.</li> <li><code>Paste</code> pastes files that were copied or cut.</li> </ul> Files <ul> <li><code>Modify</code> lets you change some things about the selected file.</li> <li><code>Copy</code> copies the selected file to your clipboard.</li> <li><code>Cut</code> copies the selected file to your clipboard and deletes it, so you can paste it elsewhere.</li> <li><code>Replace</code> lets you replace the selected file with a loose file.</li> </ul> </li> <li> <p>Once you have all your files set up you can export the mod with the <code>Export .MOD</code> button. The resulting file can be installed like any LBP mod with Toolkit.</p> </li> <li>If you aren't finished with your project you can save your progress to a project file. Set your project path and save it with the <code>Save Project</code> button. This will output a executable <code>.jar</code> file, when you run it, it will try to run the tool from the location the tool was ran in, when you saved the project. If the LBP Asset Studio <code>.jar</code> file isn't in that location, you will be prompted to select a new path to the tools <code>.jar</code>.</li> </ul> <p>Anything in the tool can be tweaked here, everything from simple renderer settings (like FOV, culling, Z near/far) and changing the keybinds, all the way to changing the color scheme, fonts and cursor.</p> Renderer Settings <ul> <li><code>No culling</code> disables backface culling, currently necessary due to some meshes rendering incorrectly.</li> <li><code>FPS</code> set the FPS limit of the tool.</li> <li><code>FOV</code> lets you change the FOV, default FOV is an approximation of the in game FOV.</li> <li><code>Z Near/Far</code> lets you set the near and far values for the view projection of the 3D viewer, the values have to be ridiculously high because LBP works in a pretty big space.</li> <li><code>AA Samples</code> you can increase the sample count for MSAA, use 2 to 4 if you really mind the jagged edges, using more will cost you performance.</li> <li><code>Outline Size</code> change the width of the outline when selecting things in 3D space.</li> <li><code>Outline Color</code>  change the color of the outline when selecting things in 3D space.</li> <li><code>Border Colors (1-4)</code> change the color of the level bounds, each is of a different layer set.</li> <li><code>Earth Color</code> change the color of the Earth helper</li> <li><code>Pod Color</code> change the color of the Pod helper</li> </ul> GUI Settings <ul> <li><code>Gui Size</code> scale the GUI down or up if the text is too big or small.</li> <li><code>Backdrop/Primary/Secondary/Tertiary Colors</code> change the color scheme of the GUI.</li> <li><code>Font Color</code> change the font color.</li> <li><code>Font Header/Text</code> change the font for headers and normal text, Comfortaa and GNU Unicode are available. GNU Unicode is less visually pleasing, however it supports every glyph, from European, Asian and many more languages. Useful when using translations.</li> <li><code>Show FPS</code> shows the FPS count of the tool in the title bar of the window.</li> <li><code>Native File Dialogue</code> uses a Java native file dialogue when opening or saving files. Useful on operating systems that aren't supported by TinyFD.</li> </ul> Fonts, Cursors <p>Not that anyone will ever do this, but the tool also has the capability to load external fonts and cursors. They need to be in a specific format, and be placed in a specific directory relative to the <code>.jar</code>. If for whatever reason you are interested in changing those, contact me on Discord and I'll explain how (not easy). Your custom font or cursor can also be added to the base tool if you're interested.</p> Controls <ul> <li>Keybinds can be changed by clicking the button next to the bind you want to change, a new screen will appear where you can set any keyboard or mouse button as the new bind.</li> <li><code>Move speed</code> changes the directional move speed, can also be increased/decreased by scrolling.</li> <li><code>Sensitivity</code> changes the mouse sensitivity when dragging to rotate the view.</li> </ul> Performance <ul> <li><code>Disable blur</code> disables the blur effect behind certain GUI elements, improves performance</li> <li><code>Disable SSAO</code> disables the ambient occlusion pass, improves performance</li> <li>Thread sleep time, can be increased to decrease CPU load, the <code>Loader</code> thread should be on a rather low sleep time, otherwise visual glitches may become noticeable.</li> </ul>"},{"location":"LBP_SRUtil/","title":"LBP Speedrun Util","text":"<p>page not finished.</p>"},{"location":"LBP_mat_mesh_converter/","title":"LBP Material Mesh Converter","text":"<p>page not finished.</p>"}]}